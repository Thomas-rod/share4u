<%= render 'shared/navbar' %>
<div id="onboarding-container">
  <div id="onboarding-container--bg"></div>
  <div class="card-onboarding fade-in">
    <!-- <div class="card-onboarding--bg"></div> -->
    <div id="onboarding-container--header">
      <h1>Bienvenu dans la Joombo Family !</h1>
      <p class=" light-black">Mais, qui es-tu ? On aimeraît apprendre à te connaître</p>
    </div>
    <div id="onboarding-container--body">
      <%= simple_form_for @profile, url: profiles_path, method: :post do |f|%>
      <div class="input-flex">
        <div class="input-flex--name">
          <%= f.input :first_name, label: "Ton prénom" %>
          <%= f.input :last_name, label: "Ton nom" %>
        </div>
        <div class="input-flex--pp">
          <%= f.input :profile_picture, input_html: { class: 'hidden', id: 'photo-input'}, label_html: { class: 'upload-photo'}, label: '📷 Ajouter une photo'   %>
          <span><%= image_tag "", width: 100, height:100, class: 'hidden', id: "photo-preview" %></span>
        </div>
      </div>
        <%= f.button :submit, "Rejoindre ", class:"button main-button" %>
      <% end %>
    </div>
  </div>
</div>
